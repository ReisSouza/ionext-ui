import { styled } from '@ionext-ui/react'

export const ActiveAccountContainer = styled('div', {
  display: 'flex',
  width: '100%',
  flexDirection: 'column',
  gap: '$4',
})

export const reforgedPassword = () => {}
