{
  "root": true,
  // This tells ESLint to load the config from the package `eslint-config-acme`
  "extends": ["@ionext-ui/eslint-config"],
  "settings": {
    "next": {
      "rootDir": ["src/*/"]
    }
  }
}
